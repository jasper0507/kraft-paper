# Kraft Paper

- **Install:** copy `kraft-paper.css` / `kraft-paper-dark.css` into Typora themes.
- **Edit source:** `src/tokens/{light,dark}.css`, `src/structure.css`, `src/dark-only.css`.
- **Build:** `npm run build` → monofiles + README palette table.
- **Check:** `npm test`.
